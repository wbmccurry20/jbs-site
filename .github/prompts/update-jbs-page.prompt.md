---
description: "Update an existing JBS website page while preserving Astro, Tailwind, SEO, and brand conventions."
---

You are updating an existing page in the JBS Construction website.

Before editing source files, confirm the repo is on a feature branch. If it is on `main`, `master`, `production`, or a release branch, stop and ask to create or switch to a feature branch first.

First inspect the relevant page, shared layout, nearby components, and global styling. Then make the requested change using the repo's existing patterns.

Requirements:

- Preserve the JBS visual style and brand voice.
- Keep the change responsive and accessible.
- Update metadata if the page meaning changes.
- Use existing assets from `public/images` when imagery is needed.
- Avoid unrelated refactors.
- Run `npm run build` when feasible.

Finish with a short summary of the page change and the verification result.